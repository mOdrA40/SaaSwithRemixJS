import type { Config } from "@react-router/dev/config";

export default {
  ssr: true,
  // Enable type generation for better TypeScript support
  future: {
    // Enable future flags for better performance and features
  },
} satisfies Config;
